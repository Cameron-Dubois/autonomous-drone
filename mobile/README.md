# Mobile app (Expo / React Native)

Control and connect UI for the project drone. **BLE** (DroneBLE) and **Wi‑Fi** utilities live under `src/comms/`.

For BLE and native Wi‑Fi APIs, use a **development build** (not Expo Go) — run `npx expo run:android` / `npx expo run:ios`, or build with EAS.
